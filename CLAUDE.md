# CLAUDE.md

See [`AGENT.md`](./AGENT.md) — same instructions, no Claude-specific
additions yet. Read it first; it points at everything else (`docs/`,
`MEMORY.md`, `CHANGELOG.md`).

If a Claude-specific convention is ever needed that shouldn't apply to
other agents, add it here rather than in `AGENT.md`. Until then, keep this
file thin — one meaning, one place.
